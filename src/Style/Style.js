import { makeStyles } from "@material-ui/core";

const toDoAction =makeStyles((theme)=>({
    toDoContainer:{
        display:'flex',
        justifyContent:'center',
        alignContent:'center',
        
    }
    }));

    export{toDoAction}